Problem Statement: Launch jenkins and explore creating projects and users

Installing Jenkins in EC2:
Step 1:
Install Java:
$sudo apt update
$sudo apt install fontconfig openjdk-17-jre
$java -version
Step 2:
Install Jenkins:
$sudo wget -O /usr/share/keyrings/jenkins-keyring.asc \
  https://pkg.jenkins.io/debian-stable/jenkins.io-2023.key
$echo "deb [signed-by=/usr/share/keyrings/jenkins-keyring.asc]" \
  https://pkg.jenkins.io/debian-stable binary/ | sudo tee \
  /etc/apt/sources.list.d/jenkins.list > /dev/null
$sudo apt-get update
$sudo apt-get install jenkins
Step 3:
Integrating Jenkins with docker:
$usermod -aG docker ubuntu(Username)
$usermod -aG docker jenkins
Step 4:
Launching jenkins:
On launching Jenkins for the 1st time, user will be getting the below screen,
